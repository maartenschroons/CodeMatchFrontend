import { UserWithPermissions } from './user-with-permissions.model';

describe('UserWithPermissions', () => {
  it('should create an instance', () => {
    expect(new UserWithPermissions()).toBeTruthy();
  });
});
