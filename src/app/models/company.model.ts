export class Company {
    constructor(public userID: number, public name: string, public location: string, public email: string, public password: string, public phonenumber: string, public biography: string) { }
}
